/* Erlaubt die Nutzung von import und export und stellt euer Projekt auf private.ERLEDIGT
Sucht und installiert das Modul chalk: ERLEDIGT
Erstellt die Datei .gitignore und fügt eine Zeile mit dem Inhalt node_modules ein ERLEDIGT
Erstellt eine Datei logger.js, in der ihr die Funktionen logRed, logGreen und logBlue
definiert und exportiert nutzt dabei die chalk Library um den übergebenen Text in Blau,
Rot oder Grün auf der Konsole auszugeben.ERLEDIGT
Erstellt eine Datei index.js, in der ihr die Funktionen logRed, logGreen und logBlue aus
logger.js importiert und verwendet, um beliebigen Text bunt auszugeben.
Bevor ihr committed: Prüft, dass SourceTree (bzw. die Git-UI eurer Wahl) keine Dateien
aus dem Ordner node_modules committen möchte, sonst stimmt etwas mit eurer
.gitignore Datei nicht. Ihr dürft Dateien aus node_modules unter keinen Umständen
committen!ERLEDIGT
 */

